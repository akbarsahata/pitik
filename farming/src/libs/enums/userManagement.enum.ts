/* eslint-disable no-unused-vars */
export enum HTTP_METHOD_ENUM {
  GET = 'GET',
  POST = 'POST',
  PATCH = 'PATCH',
  PUT = 'PUT',
  DELETE = 'DELETE',
}

export enum PRESET_TYPE_ENUM {
  ROLE = 'ROLE',
  PRIVILEGE = 'PRIVILEGE',
}

export enum LOGIN_ACTION {
  DEFAULT_PASSWORD = 'DEFAULT_PASSWORD',
  WEAK_PASSWORD = 'WEAK_PASSWORD',
  BREACHED = 'BREACHED',
}
